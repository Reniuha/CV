Environment Variables  
The .env file contains a temporary MongoDB Atlas connection string.
It is provided only for demonstration purposes and will be removed once the project showcase is complete.

How to Run the Project

Backend:

Open the backend directory.

Install dependencies using:
npm install

Start the backend server using:
node server.js

Frontend:

Open the frontend directory.

Install dependencies using:
npm install

Start the development server using:
npm start
